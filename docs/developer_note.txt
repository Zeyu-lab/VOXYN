VOXYN v0.3
Dashboard UI rebuilt:
- fixed full-size sidebar layout
- restored blue/white dashboard style
- added create/join room panel
- added max members 5/8/10
- added recent rooms / room blueprint / server access sections

VOXYN v0.31 Update

Finished the first real Profile system prototype.

Updates:
- Added a dedicated Profile.vue page.
- Added /profile route.
- Changed Dashboard email pill into a circular profile button.
- Built Compact Glass Profile layout.
- Added real avatar upload through Supabase Storage.
- Created avatars bucket with user-folder based upload policy.
- Saved avatar_url, avatar_path, and avatar_updated_at into Supabase Auth metadata.
- Synced uploaded avatar back to the Dashboard topbar profile circle.
- Added fallback initial display if avatar image fails to load.

VOXYN v0.311 Update
- Delete old avatar path replace with new without cost extra memory

Voxyn v0.4 update
VOXYN from a basic Express backend into a realtime backend using Socket.IO.

Updates:
- Added Socket.IO backend support.
- Added realtime room join logic.
- Added realtime chat message sending.
- Added room leave and disconnect cleanup.
- Added temporary in-memory room storage.
- Added recent message history for each active room.
- Added online user tracking per room.
- Connected RoomView chat input to the backend socket events.
- Fixed setup issues with frontend socket import and Supabase client sharing.
- Confirmed that localhost:5173 is only a development origin, not a permanent requirement.

v0.41 update:
- Optimized the VOXYN sidebar / rail navigation buttons
- Adjustment the size regarding has in half and full screen

v0.45 Update
- Fixed Dashboard sidebar interaction behavior
- Matched Dashboard sidebar logic with RoomView navigation style
- Added shared navigation logic for sidebar and mobile menu
- Improved half-screen responsive behavior
- Added mobile menu backdrop close behavior
- Prepared route entry for Profile and Settings
- Reduced inconsistency between Dashboard and RoomView layouts

v0.46 update
- Fixed Profile metadata synchronization across VOXYN
- Standardized Supabase user metadata keys for display name, status message, and avatar
- Updated Dashboard to read the latest Profile identity from Supabase Auth
- Removed hardcoded profile initials such as Z / U from identity display areas
- Added avatar cache-busting with avatar_updated_at
- Improved Profile avatar upload flow and metadata update order
- Prepared identity sync foundation for RoomView, backend socket users, and chat avatars